overwrite: true
schema: "http://localhost:8009/graphql"
documents: null
generates:
  src/gql/types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      scalars:
        Date: Date
        ID: string
