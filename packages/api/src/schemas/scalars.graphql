scalar Date
scalar ID

directive @auth(requires: [Role!] = [USER]) on QUERY | FIELD_DEFINITION

enum Role {
    ADMIN
    USER
}

enum GeneralOrderType {
  RecentFirst
  RecentLast
  NameAZ
  NameZA
}

enum Days {
  Monday
  Tuesday
  Wednesday
  Thursday
  Friday
  Saturday
  Sunday
}